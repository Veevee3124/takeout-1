'use strict'
require('./check-sd')()
cvffd
mspinner.start()mng
nn      colors: as,nn
g      children: fnnnalse, // If n are using ts-loader, setting this to true will make TypeScript errors show up during build.
vggg      nn:dbn false
    }) + '\n\n')nnbbb
ssssddaa
n      console.log(nn.red('  Build failed with errors.\n'))
n    }
dd    console.log(chalk.d'  Build complete.\n'))
    console.log(chalk.yellow(
      '  Tip: built ssss aresaa meant to be served over an HTTP server.\n' +
      '  Opening index.html over file:// won\'t work.\n'
    ))
  })
})
